{
  "name": "server",
  "version": "1.0.0",
  "description": "Invoice app backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd client && npm cache clean –force && npm install --only=dev && npm install && npm run build"
  },
  "engines": {
    "node": "19.3.0"
  },
  "author": "Sitaram Rathi",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.3",
    "nodemailer": "^6.6.3",
    "nodemon": "^2.0.20"
  }
}
